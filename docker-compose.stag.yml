# version: '3.8'
name: 'helloabdul_stag'
services:
  # ci:
  #   container_name: ci
  #   build:
  #     context: .
  #     dockerfile: ./ci/Dockerfile
  #   ports:
  #     - '4999:4999'
  #   networks:
  #     - custom_network

  postgres_stag:
    container_name: postgres_stag
    build:
      context: .
      dockerfile: ./postgres/Dockerfile
    restart: always
    ports:
      - '5433:5432'
    env_file:
      - ./backend/.env
    volumes:
      - ./postgres/postgres_stag:/var/lib/postgresql/data
    networks:
      - custom_network

  # pgadmin_stag:
  #   container_name: pgadmin_stag
  #   build:
  #     context: .
  #     dockerfile: ./postgres/Dockerfile
  #   ports:
  #     - '60001:80'
  #   environment:
  #     - PGADMIN_DEFAULT_EMAIL=admin@example.com
  #     - PGADMIN_DEFAULT_PASSWORD=Ssg123777
  #   volumes:
  #     - ./pgadmin/pgadmin:/var/lib/pgadmin
  #   depends_on:
  #     - postgres_stag

  backend_stag: # Development environment
    container_name: backend_stag
    build:
      context: .
      dockerfile: ./backend/Dockerfile
      target: development # Use the development stage in the Dockerfile
    entrypoint: ['/bin/bash', '/app/backend/entrypoint.sh']
    env_file:
      - ./backend/.env # Use development environment variables
    ports:
      - '50002:50001'
    depends_on:
      postgres_stag:
        condition: service_started
    networks:
      - custom_network

  # crm_stag:
  #   container_name: crm_stag
  #   build:
  #     context: .
  #     dockerfile: ./crm/Dockerfile
  #     target: development
  #   env_file:
  #     - ./crm/.env # Use development environment variables
  #   ports:
  #     - '5173:5173'
  #   depends_on:
  #     - postgres_stag
  #     - backend_stag
  #   networks:
  #     - custom_network

volumes:
  pgadmin-data:

networks:
  custom_network:
    # external: true
