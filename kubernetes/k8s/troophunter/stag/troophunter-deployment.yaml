apiVersion: apps/v1
kind: Deployment
metadata:
  name: troophunter-stag
spec:
  replicas: 1
  selector:
    matchLabels:
      app: troophunter-stag
  template:
    metadata:
      labels:
        app: troophunter-stag
    spec:
      containers:
        - name: troophunter-stag
          image: 192.168.1.201/troophunter-stag:latest
          imagePullPolicy: Always
          command: ["npm", "run", "staging"]
          ports:
            - containerPort: 3001
          env:
            - name: PORT
              value: "3001"
          volumeMounts:
            - name: logs
              mountPath: /microfrontend/troophunter/logs
          resources:
            requests:
              cpu: "25m"
              memory: "100Mi"
            limits:
              cpu: "50m"
              memory: "150Mi"
      volumes:
        - name: logs
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: troophunter-stag
spec:
  selector:
    app: troophunter-stag
  ports:
    - name: troophunter-http
      protocol: TCP
      port: 3001
      targetPort: 3001