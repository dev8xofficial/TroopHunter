apiVersion: apps/v1
kind: Deployment
metadata:
  name: businesses-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: businesses-dev
  template:
    metadata:
      labels:
        app: businesses-dev
    spec:
      initContainers:
        - name: wait-for-db
          image: busybox
          command: ['sh', '-c', 'until nc -z businesses-db 5432; do echo waiting for db; sleep 2; done;']
          resources:
            requests:
              cpu: "25m"
              memory: "32Mi"
            limits:
              cpu: "50m"
              memory: "64Mi"
      containers:
        - name: businesses-dev
          image: 192.168.1.201/businesses-dev:latest
          imagePullPolicy: Always
          command: ["/bin/bash", "/app/microservices/businesses/entrypoint.sh"]
          ports:
            - containerPort: 50011
          env:
            - name: NODE_ENV
              value: development
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: businesses-db-secret
                  key: host
            - name: DB_PORT
              valueFrom:
                secretKeyRef:
                  name: businesses-db-secret
                  key: port
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: businesses-db-secret
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: businesses-db-secret
                  key: password
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: businesses-db-secret
                  key: dbname
          volumeMounts:
            - name: logs
              mountPath: /microservices/businesses/logs
          resources:
            requests:
              cpu: "25m"
              memory: "100Mi"
            limits:
              cpu: "50m"
              memory: "150Mi"
      volumes:
        - name: logs
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: businesses-dev
spec:
  selector:
    app: businesses-dev
  ports:
    - protocol: TCP
      port: 50011
      targetPort: 50011