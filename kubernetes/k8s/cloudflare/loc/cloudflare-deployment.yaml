apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflared-loc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloudflared-loc
  template:
    metadata:
      labels:
        app: cloudflared-loc
    spec:
      containers:
        - name: cloudflared-loc
          image: cloudflared-loc:latest
          args:
            - tunnel
            - --no-autoupdate
            - run
            - --token
            - /etc/cloudflared/token
          volumeMounts:
            - name: cloudflared-token
              mountPath: /etc/cloudflared/token
              subPath: token
      volumes:
        - name: cloudflared-token
          secret:
            secretName: cloudflared-token