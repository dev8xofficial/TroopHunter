apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflared-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloudflared-dev
  template:
    metadata:
      labels:
        app: cloudflared-dev
    spec:
      containers:
        - name: cloudflared-dev
          image: cloudflared-dev:latest
          args:
            - tunnel
            - --no-autoupdate
            - run
            - --token
            - /etc/cloudflared/token
          volumeMounts:
            - name: cloudflared-token
              mountPath: /etc/cloudflared/token
              subPath: token
      volumes:
        - name: cloudflared-token
          secret:
            secretName: cloudflared-token