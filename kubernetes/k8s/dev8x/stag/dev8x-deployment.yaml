apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev8x-stag
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dev8x-stag
  template:
    metadata:
      labels:
        app: dev8x-stag
    spec:
      containers:
        - name: dev8x-stag
          image: 192.168.1.201/dev8x-stag:latest
          imagePullPolicy: Always
          command: ["npm", "run", "staging"]
          ports:
            - containerPort: 3004
          env:
            - name: PORT
              value: "3004"
          volumeMounts:
            - name: logs
              mountPath: /microfrontend/dev8x/logs
          resources:
            requests:
              cpu: "25m"
              memory: "100Mi"
            limits:
              cpu: "50m"
              memory: "150Mi"
      volumes:
        - name: logs
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: dev8x-stag
spec:
  selector:
    app: dev8x-stag
  ports:
    - name: dev8x-http
      protocol: TCP
      port: 3004
      targetPort: 3004