apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-stag
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users-stag
  template:
    metadata:
      labels:
        app: users-stag
    spec:
      initContainers:
        - name: wait-for-db
          image: busybox
          command: ['sh', '-c', 'until nc -z users-db 5432; do echo waiting for db; sleep 2; done;']
          resources:
            requests:
              cpu: "25m"
              memory: "32Mi"
            limits:
              cpu: "50m"
              memory: "64Mi"
      containers:
        - name: users-stag
          image: 192.168.1.201/users-stag:latest
          imagePullPolicy: Always
          command: ["/bin/bash", "/app/microservices/users/entrypoint.sh"]
          ports:
            - containerPort: 50016
          env:
            - name: NODE_ENV
              value: staging
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: users-db-secret
                  key: host
            - name: DB_PORT
              valueFrom:
                secretKeyRef:
                  name: users-db-secret
                  key: port
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: users-db-secret
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: users-db-secret
                  key: password
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: users-db-secret
                  key: dbname
          volumeMounts:
            - name: logs
              mountPath: /microservices/users/logs
          resources:
            requests:
              cpu: "25m"
              memory: "100Mi"
            limits:
              cpu: "50m"
              memory: "150Mi"
      volumes:
        - name: logs
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: users-stag
spec:
  selector:
    app: users-stag
  ports:
    - protocol: TCP
      port: 50016
      targetPort: 50016