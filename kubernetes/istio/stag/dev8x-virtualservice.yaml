apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: dev8x-stag
spec:
  hosts:
    - "dev8x.stag"
  gateways:
    - istio-system/main-gateway
  http:
    - match:
        - uri:
            prefix: "/"
      route:
        - destination:
            host: dev8x-stag
            port:
              number: 3003
      rewrite:
        uri: "/"
      headers:
        response:
          add:
            x-frontend-catchall: "true"