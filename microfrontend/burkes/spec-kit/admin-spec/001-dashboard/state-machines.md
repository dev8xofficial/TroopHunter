# Dashboard State Machines

*The Dashboard module is a purely read-only aggregation interface mapping state values from underlying domain entities. It does not introduce novel state machines of its own.*
