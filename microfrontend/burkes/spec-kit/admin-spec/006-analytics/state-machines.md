# Analytics State Machines

*The Analytics module operates as a projection layer over historical immutable data spanning all other domains. It calculates deltas dynamically on fetch. It introduces no local state mutability or state machines.*
