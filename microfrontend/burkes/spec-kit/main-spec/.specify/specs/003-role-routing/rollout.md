# Role Routing — Rollout Plan

## 1. Sub-Portal Alignment
Before updating the Destination URLs, we must ensure all target portals (`attorney.html`, `cpa.html`, etc.) are actively deployed and capable of receiving the hand-off tokens.
