# MFA — Rollout Plan

## 1. Feature Dependencies
Requires Backend API to dispatch codes out-of-band securely.

## 2. Rollout
Deploy as a standard DOM hidden element controlled by `auth-screen` state management.
