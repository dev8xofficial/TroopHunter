# Rollout Plan: CRM Email

- Begin with a small internal cohort using linked Outlook accounts.
- Validate inbound linking and unresolved-sender resolution before broad rollout.
- Keep send actions independently controllable from read-only inbox visibility during rollout.
