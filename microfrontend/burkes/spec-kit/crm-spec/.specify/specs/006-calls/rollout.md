# Rollout Plan: CRM Calls

- Validate provider connectivity in a controlled internal pilot.
- Enable read-only call log before broad dialing if needed.
- Review recording compliance behavior with department owners before general rollout.
