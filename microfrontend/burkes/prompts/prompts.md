I have attached several documents and transcript files to this chat. Based on these, I have generated five HTML files: `admin.html`, `agent.html`, `attorney.html`, `client.html`, and `cpa.html`. For this session, please focus exclusively on the Transactions and Documents modules. Using the information gathered from the attached files, validate how these modules are created and managed within the HTML files according to the following criteria:
* Data Efficiency: Ensure that the Transactions and Documents modules do not use any unnecessary data fields in forms, tables, or any other part of the interface.
* Consistency: Verify that these modules use identical headings, subheadings, labels, and naming conventions across all portals. The goal is to standardize concepts and fields to improve the user experience and eliminate redundant terminology.
* Role-Based Access: Confirm that each user role is correctly defined to update transaction statuses and documents. Every portal must provide clear, role-specific actions for users to perform their required tasks.

Rather than rewriting all five complete files, the right deliverable is a precise patch document containing only the changed HTML blocks per file, plus a clear audit report.

Now, you have to read Admin and Agent changes from audit_and_patch.md and implement them in admin.html and agent.html files.

======================================================================================================================================================

I have attached several documents and transcript files to this chat. Based on these, I have generated five HTML files: `admin.html`, `agent.html`, `attorney.html`, `client.html`, `cpa.html`, `servicePartner.html`, `index.html` and `mortgageLender.html`. For this session, please focus exclusively on the Transactions and Documents modules. Using the information gathered from the attached files, validate how these modules are created and managed within the HTML files according to the following criteria:
* Role-Based Access: Confirm that each user role is correctly defined to update transaction statuses. Every portal must provide clear, role-specific actions for users to perform their required tasks. Specially focus on CPA and Attorney.

Rather than rewriting all five complete files, the right deliverable is a precise patch document containing only the changed HTML blocks per file, plus a clear audit report.

Now, you have to read Admin and Agent changes from audit_and_patch.md and implement them in admin.html and agent.html files.

======================================================================================================================================================

Read CPA changes from audit_and_patches.md and implement into cpa.html file.

======================================================================================================================================================

Read Agent, Client, mortgageLender and servicePartner changes from audit_and_patches.md and implement into` agent.html`,` client.html`, `mortgageLender.html` and `servicePartner.html` files.

======================================================================================================================================================

Task: Analyze the attached 7 HTML portal files (Admin, Agent, Client, Attorney, CPA, Mortgage Lender, and Service Partner) to create a unified, synchronized dataset for demonstration.

Instructions:
1. Data Extraction: Scan all files to identify every data field used in tables, status badges, and profile sections. Specifically, look for shared entities like "Client Name," "Property Address," "Transaction ID," and "Agent Name."
2. Identify Cross-Portal Links: Map how data moves between files. (e.g., If a transaction is created in `admin.html`, it must appear in `client.html` and `agent.html`).
3. Synthetic Data Generation: > - Determine the optimal number of unique users (Admins, Agents, Clients, etc.) needed to populate all portals realistically.
   * Generate a cohesive dataset where names, emails, and transaction details are identical and consistent across every file.
4. Output Format: Provide the final data in a structured format (like JSON) that I can easily plug into the HTML templates to replace the current placeholders.

======================================================================================================================================================

Use the data from unified_demo_data.json in cpa.html, mortgageLender.html and servicePartner.html files.

======================================================================================================================================================


======================================================================================================================================================


======================================================================================================================================================


======================================================================================================================================================