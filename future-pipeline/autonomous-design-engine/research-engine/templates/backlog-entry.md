# Backlog Entry — `<B-id>`

> One row in the permanent Backlog. Every item generated during divergence becomes one of these — **nothing generated is discarded** ([`../01 §4.3`](../01-operating-model.md)). Store these compactly (a table in a per-Area file or one shared backlog file); this is the expanded shape.

---

- **ID:** `B-...`
- **Area(s):** `A-...` *(tag; may point at a linked Area)*
- **Item:** *(the question / hypothesis / attack / analogy — as generated, verbatim)*
- **Type:** question / hypothesis / attack / cross-domain-analogy / simplification
- **EVI factors:**
  - impact_if_true (1–5): `_`
  - probability_true (0–1): `_`
  - reversibility_of_acting (0.2–1): `_`
  - cost_to_investigate (1–5): `_`
  - **EVI = (impact × probability × reversibility) / cost = `_`**
- **Status:** open / investigating / answered / obsolete
- **Resolution link:** *(finding / Decision Record id, once answered — or reason if obsolete)*
- **Generated by:** *(investigation id + date)*
