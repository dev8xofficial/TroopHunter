# Area Card — `<A-id>` `<Title>`

> Copy this to `areas/<A-id>.md` when an Area moves to **Active**. This is the **only** Area-specific artifact; everything downstream is generic. Keep it tight — it frames one focused investigation, not an essay.

---

## Identity
- **ID:** `A-...`
- **Kind:** subsystem / cross-cutting / decision / mechanism / new
- **Mode:** paper / empirical / mixed — *(what evidence is even reachable today)*
- **Parent Area:** `<A-id or none>`
- **Linked Areas:** `<A-id, A-id>` — *(known dependencies / interactions)*
- **Proportionality (initial):** Light / Standard / Deep — *(start Light unless stakes are obvious)*

## 1. What this area is
One paragraph: the boundary of this Area. What is *in*, and explicitly what is *out* (belongs to a linked Area).

## 2. Why it matters
What does ADE's autonomy / quality / robustness / cost depend on here? What breaks downstream if this area is wrong?

## 3. Current understanding (reconstruct, don't copy)
How the mechanism actually works, rebuilt from first principles. Where the spec and your reconstruction disagree, note it — that gap is already a finding.

## 4. Assumptions this area rests on
List them, especially the implicit ones. Label each: `evidenced` / `intuited` / `untested` / `unfalsifiable`.

## 5. Entry questions (seed only — divergence is unbounded)
A few starting questions to prime the investigation. **These do not limit it** — stage 2 generates without cap ([`../01-operating-model.md §4`](../01-operating-model.md)); this list is a launch point, not a fence.

## 6. Known evidence available
What instruments ([`../04-instruments.md`](../04-instruments.md)) can this Area reach *now*? Which reach only T0/T1 (paper), which can reach T3 (has code + benchmark)?

## 7. Prior knowledge
Related Knowledge-Base entries and past investigations to build on (so we don't relearn).
