---
- name: Fail early if no model file was specified
  ansible.builtin.assert:
    that: llama_model_file | length > 0
    fail_msg: "Set llama_model_file (e.g. Ornith-1.0-35B-Q4_K_M.gguf) in inventory or -e."

- include_tasks: build.yml
- include_tasks: model.yml
- include_tasks: service.yml
