---
- name: Move into the TroopHunter directory and run npm run start:{{ ENVIRONMENT }}
  command: bash -c "cd /home/{{ USER }}/{{ PROJECT_NAME }}/{{ ENVIRONMENT }}/ && source /home/{{ USER }}/.nvm/nvm.sh && npm run start:{{ ENVIRONMENT }}"
# - name: Move into the TroopHunter Landing project and serve production build
#   command: bash -c "cd /home/{{ USER }}/{{ PROJECT_NAME }}/{{ ENVIRONMENT }}/microfrontend/troophunter-landing/dist && npx serve -s . -l 3000"
- name: Move into the TroopHunter directory and run npm run start:microfrontend
  command: bash -c "cd /home/{{ USER }}/{{ PROJECT_NAME }}/{{ ENVIRONMENT }}/ && source /home/{{ USER }}/.nvm/nvm.sh && npm run start:microfrontend"
# - name: Move into the TroopHunter directory and run npm run start:microservices
#   command: bash -c "cd /home/{{ USER }}/{{ PROJECT_NAME }}/{{ ENVIRONMENT }}/ && source /home/{{ USER }}/.nvm/nvm.sh && npm run start:microservices"