---
- name: Update apt cache
  apt:
    update_cache: yes

- name: Install NGINX
  apt:
    name: nginx
    state: present

- name: Ensure NGINX is running
  service:
    name: nginx
    state: started
    enabled: yes

- name: Configure NGINX with a simple configuration
  copy:
    dest: /etc/nginx/sites-available/default
    content: |
      server {
        listen 80;
        listen [::]:80;
        server_name nginx.troophunter.com;
        
        location / {
          proxy_pass http://192.168.1.200:50000;
          proxy_http_version 1.1;
          proxy_set_header Upgrade $http_upgrade;
          proxy_set_header Connection 'upgrade';
          proxy_set_header Host $host;
          proxy_cache_bypass $http_upgrade;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
      }

- name: Create symbolic link for site configuration
  file:
    src: /etc/nginx/sites-available/default
    dest: /etc/nginx/sites-enabled/default
    state: link

# - name: Remove default site configuration
#   file:
#     path: /etc/nginx/sites-enabled/default
#     state: absent
#     force: yes

# - name: Test NGINX configuration
#   command: nginx -t
#   register: nginx_test
#   failed_when: nginx_test.rc != 0
#   changed_when: false

- name: Reload NGINX
  service:
    name: nginx
    state: reloaded

